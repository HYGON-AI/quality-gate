# v2.0.7 approved scope

- Rename display names to Code compliance, Code quality, Code security.
- Preserve matrix/check IDs and All required checks for branch protection.
- Remove Lizard execution, YAML formatting diagnostics, ShellCheck style and CRLF advisories.
- Retain ShellCheck info/warnings about behavior, syntax, duplicate keys and operational failures.
- Semgrep targets only Python/JS/TS; excluded languages must not be presented as scanned.
- Keep existing images and compliance, secrets and security finding severity policies.
- Ruff configuration isolation and trustworthy changed-line calculation are deferred, known limitations.

Acceptance: unit/self tests, Linux bootstrap, real pinned-tool positive/negative cases,
CI on the candidate and merged commit, then immutable v2.0.7 tag/release and stable update.
No release if required checks or review rules remain unsatisfied; do not bypass protection.
