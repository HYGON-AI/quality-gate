# v2.0.6 — C++ 解析兼容性告警精确降级

- 修复 rocSHMEM-das PR #33 因 Semgrep 已知 C++ 解析兼容性告警导致门禁阻断。
- 仅匹配已验证的错误类型、code 2、warn 级别、C++ 扩展名、匹配路径及精确 simple-var 文案；显示原始位置。
- 明确展示“覆盖不完整”，不宣称未解析部分已获安全验证，不跳过整个文件或删除其他安全发现。
- 未知解析异常、进程故障、缺失/损坏报告继续阻断；真实阻断项保持原级别。
- 未升级 Semgrep 镜像：候选 1.177.0 仍复现同一错误。
- 保留现有四个 Job 和调用接口；已有固定版本标签不变。

验证：Linux 139 项单元测试通过（含 13 项专项测试）；生产镜像对原文件的候选执行器重放得到 0 阻断、1 项带位置的兼容性提示。
线上候选与发布后验证记录在完成后补充；本地重放不代表已完成 GitHub PR 验证。
